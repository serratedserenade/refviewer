[project]
name = "refviewer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyinstaller>=6.20.0",
    "pyqt6>=6.11.0",
]

[dependency-groups]
dev = [
    "watchfiles>=1.1.1",
]
